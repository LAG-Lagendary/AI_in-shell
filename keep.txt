📌 Ключевые моменты, которые стоит помнить для вашего проекта "AI in Shell":

    Принцип работы LLM: Модель — это предсказатель следующего токена. Чтобы она выдала команду, нужно сделать эту команду самым вероятным следующим токеном.

    Структура Проекта: Ваша задача — написать скрипт-оболочку (shell script или Python/Go скрипт), который будет:

        Отправлять запрос пользователя плюс описание ваших алиасов (функций) в LLM.

        Принимать от LLM строго структурированный JSON (вашу "команду").

        Исполнять эту команду в терминале (например, вызывать mv, rm или mkdir).

        Возвращать результат (успех/неудача) обратно в LLM для формирования финального ответа пользователю.

    Ключ к успеху: Промпт для Алиаса (JSON Schema): Вам потребуется определить формат JSON, который модель должна возвращать. Например:
    JSON

{
  "action": "ls",
  "arguments": {
    "path": "playground",
    "options": "-l"
  }
}

Именно это описание (схема) вы будете загружать как "алиас" в LLM.
